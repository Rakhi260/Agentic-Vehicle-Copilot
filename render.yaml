services:
  - type: web
    name: agentic-vehicle-copilot
    runtime: python
    plan: free

    buildCommand: pip install -r requirement.txt

    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT